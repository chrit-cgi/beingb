/**
 * Analysis service entry point.
 * Add new analysis tasks as separate files in this directory
 * and export them here.
 *
 * Example future tasks:
 *   export { summarizeReports } from "./summarize";
 *   export { detectPatterns } from "./patterns";
 */
